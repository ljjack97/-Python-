# CLAUDE.md — CAD 图像转代码工具

## 项目概述

将建筑平面图 / 园林景观设计图自动识别并转换为 AutoCAD `.scr` 脚本的 Windows 桌面应用。

- **语言**: Python 3.12
- **UI 框架**: PyQt5
- **图像识别**: OpenCV 传统 CV（非深度学习）
- **目标 CAD**: AutoCAD 2020+

---

## 项目进度

| 阶段 | 内容 | 文件数 | 状态 |
|------|------|--------|------|
| 阶段1 | 基础设施 | 5 | ✅ |
| 阶段2 | 图像处理核心 | 6 | ✅ |
| 阶段3 | 高层检测器 | 3 | ✅ |
| 阶段4 | CAD 代码生成 | 4 | ✅ |
| 阶段5 | UI 构建 | 7 | ✅ |
| 阶段6 | 自动化导出 | 4 | ✅ |
| 阶段7 | 打磨 | 4 | ✅ |
| 阶段8 | 修复优化 | 修复 SCR + UI 优化 | ✅ |
| **总计** | | **34** | |

### 测试状态
- 单元测试: 8/8 通过 ✅

---

## 已知问题

### 1. CAD 图纸识别率低 ⚠️
- **现象**: 用户的实际 CAD 图纸检测不到特征
- **原因**: 检测参数未针对用户的图纸类型调优
- **待办**: 需要用户提供样本图纸，针对性调优参数

### 2. ~~中文路径加载~~ ✅ 已修复 (2026-06-01)
- `cv2.imread()` → `np.fromfile + cv2.imdecode`

### 3. ~~设置按钮闪退~~ ✅ 已修复 (2026-06-01)
- `settings_dialog.py` 缺少 `QWidget` 导入

### 4. ~~SCR 代码无法在 AutoCAD 运行~~ ✅ 已修复 (2026-06-01)
- SCR 注释全部改为英文 ASCII
- 移除 SETVAR，改用直接变量名
- 图层创建命令使用显式换行格式 `-LAYER\nN\nWalls\n\nC\n7\nWalls\n`
- `-LAYER S` 命令添加退出回车

### 5. ~~UI 布局~~ ✅ 已优化 (2026-06-01)
- 分割比例 7:3（图片:代码）
- 添加缩放控件（滚轮/+/−/适应）
- 双击切换原图/叠加层
- 框选区域检测功能
- 导出模式下拉框样式优化
- 使用说明按钮（F1 快捷键）

---

## 工作日志

### 2026-06-01 (第二轮修复)

**SCR 代码修复:**
- 将所有 SCR 注释改为英文 ASCII（AutoCAD SCR 不支持中文）
- 移除 `SETVAR` 命令，改用直接系统变量名（`OSNAPCOORD\n1`）
- 修复 `-LAYER` 命令格式：使用显式 `\n` 分隔，在 `New` 子提示后加额外回车退出
- `-LAYER S` 命令末尾加回车退出命令
- 代码生成器改为按图层分组，减少图层切换次数
- 所有命令验证为 ASCII-only

**UI 优化:**
- 添加缩放控件栏（− / 百分比 / + / 适应按钮）
- 实现滚轮缩放（10%~400%）
- 添加框选区域功能（右键或按钮触发），支持只检测框内区域
- 添加使用帮助对话框（F1 或工具栏按钮），含完整 HTML 说明书
- 工具栏添加绿色"使用说明"按钮
- 图片面板占位提示更新

**测试:**
- 更新测试以验证 ASCII-only 的 SCR 输出
- 8/8 测试通过

### 2026-06-01 (第一轮)

- [x] 完成项目初始化，创建完整项目结构（32个文件）
- [x] 实现图像预处理流水线
- [x] 实现直线/圆/弧/轮廓检测
- [x] 实现比例尺自动识别
- [x] 实现墙体/门窗/景观检测
- [x] 实现 CAD 命令模板和代码生成
- [x] 实现三种导出模式
- [x] 实现 PyQt5 UI（淡蓝色主题，中文界面）
- [x] 修复中文路径加载 bug
- [x] 修复设置对话框闪退 bug

---

## 关键技术决策

1. **传统 CV 而非深度学习** — 预训练工程图纸模型难以获取
2. **np.fromfile + cv2.imdecode** — 解决 Windows 中文路径
3. **QThread 后台处理** — 防止大图 UI 卡顿
4. **临时文件 + SCRIPT 命令** — COM 模式可靠执行方式
5. **SCR 纯 ASCII** — AutoCAD 脚本不支持中文注释

---

## 下一步

1. **参数调优** — 针对用户实际图纸优化检测参数
2. **手动标注** — 允许手动标记检测失败的区域
3. **打包 .exe** — PyInstaller 打包独立可执行文件
